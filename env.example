# Used by Nhost Functions (server-side only).
#
# Note: this repo avoids committing `.env*` because some tooling globally ignores them.
# Copy this file to your preferred local env mechanism.

# Hasura (server-only admin operations)
HASURA_GRAPHQL_ADMIN_SECRET=replace-me
HASURA_GRAPHQL_URL=http://localhost:1337/v1/graphql

# Upstash Redis (optional but recommended for rate limiting + caching)
UPSTASH_REDIS_REST_URL=replace-me
UPSTASH_REDIS_REST_TOKEN=replace-me

# Google photo proxy
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Upload (if keeping S3 + Sharp pipeline)
S3_ACCESS_KEY_ID=replace-me
S3_SECRET_ACCESS_KEY=replace-me
S3_REGION=ap-northeast-2
S3_BUCKET_NAME=replace-me
S3_BUCKET_DOMAIN=replace-me

# Image optimization defaults
IMAGE_MAX_WIDTH=1600
IMAGE_MAX_HEIGHT=1600
IMAGE_AVIF_QUALITY=60
IMAGE_WEBP_QUALITY=75
